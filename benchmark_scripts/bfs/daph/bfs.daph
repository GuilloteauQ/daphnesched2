G = readMatrix($f);
maxi = 200;
n = nrow(G);
x = fill(0.0, n, 1);
x[0,0] = [1.0];
one = fill(1.0, n, 1);

start=now();
for(iter in 1:maxi) {
  x = min(one, x + G @ x);
}
end=now();
print((end-start) / 1000000000.0);
